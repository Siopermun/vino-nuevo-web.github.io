# Ejemplo de variables de entorno para producción
# Estas variables deben configurarse en el panel de control de tu hosting, no en este archivo.

VITE_REACT_APP_API_URL=https://api.tudominio.com # Reemplaza con la URL de tu API en producción
# Otras variables de entorno que tu backend pueda necesitar (ej. para la base de datos, JWT secret)
# DB_HOST=your_db_host
# DB_USER=your_db_user
# DB_PASS=your_db_password
# JWT_SECRET=your_jwt_secret_key
